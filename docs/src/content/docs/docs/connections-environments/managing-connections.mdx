---
title: Managing connections
description: Point Renart at your warehouses and organize them into environments.
---

A **connection** tells Renart how to reach a data platform — a local DuckDB
file, a Postgres database, a Snowflake or Databricks warehouse, or S3-compatible
or Google Cloud Storage. An **environment** is a named set of connections (for
example `default` and `production`) so the same pipeline can run against
different targets.

Both live in the project config file in your repository, and both are
editable in the UI: open the project switcher and choose **Project
settings**, then the **Environments** and **Connections** sections.

## Add a connection

In **Project settings → Connections**, add a connection with a name (for
example `duckdb-default` or `pg-prod`) and its credentials. Assets refer to
connections by name — the workbench shows which connection an asset runs
against, and the pipeline's defaults live in `pipeline.yml`.

Sensitive values are write-only. By default Renart stores a replacement in the
operating system's credential store and keeps only a symbolic placeholder in
the project config. The connection editor also supports an encrypted local
vault for interactive SSH or headless sessions and environment-variable
bindings for CI and unattended runs. See
[Manage connection credentials](/docs/connections-environments/managing-credentials/)
for the complete setup, rotation, and recovery guidance.

## Environments

In **Project settings → Environments**, define one entry per target. Each
environment maps connection names to concrete credentials, so switching the
environment picker in the top bar swaps the whole workspace — previews,
materializations, and ad-hoc queries — onto that target. Schedules are also
per environment: the same pipeline can run nightly against `default` and on
weekday mornings against `production`.

## Supported platforms

Renart executes against supported warehouses including DuckDB, Postgres,
Snowflake, BigQuery, Redshift, and Databricks. Configured S3-compatible and GCS
connections are available to Load assets even when no connector-specific asset
integration is enabled; their object browser is explicit, bounded, and keeps
credential values on the server. The demo projects use DuckDB, which needs no
credentials at all — a good way to try the workflow before pointing at shared
infrastructure.

:::caution[Keep legacy inline credentials private]
Newly replaced sensitive values use a write-only credential source. Existing
inline values remain readable for compatibility until you replace them, so
keep any project config containing them private and out of version control.
Renart does not fall back to plaintext storage.
:::

## Related

- [Manage connection credentials](/docs/connections-environments/managing-credentials/)
- [Scheduling](/docs/scheduling/overview/)
- [Security and privacy](/docs/reference/security-and-privacy/)
